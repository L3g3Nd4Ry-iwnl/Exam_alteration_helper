<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select your slot</title>
    <link rel="stylesheet" href="./css/style.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="stylesheet" type="text/css" href="./bootstrap-5.0.0-dist/css/bootstrap.min.css">
    <script type="text/javascript" src="./bootstrap-5.0.0-dist/js/bootstrap.min.js"> </script>
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@700&display=swap" rel="stylesheet">  
    <link href="https://fonts.googleapis.com/css2?family=Raleway&display=swap" rel="stylesheet">
</head>

<body class="d-flex flex-column min-vh-100 bodyclass">
    <header class="d-flex flex-row align-items-center text-white">
        <h3 class="p-3 mr-auto">Exam Alteration Helper</h3>
        <form action="/faculty/logout" method="POST">
            <button class="btn" id="logoutbutton">Logout</button>
        </form>  
    </header>

    <main class="admindash">
        <div class="info text-center">
            <br>
            Select the slot you want to request for exchange.</br>
            Click on the radio button of the slot you want to request and click Submit!
            <br>
            <br>
        </div>
        <br>
        <div class="m-5 slot" style="overflow-x: auto;">
            <form action="/faculty/exchange/confirm" method="POST">
                <table>
                    <th>S.No</th>
                    <th>Date</th>
                    <th>Slot</th>
                    <th>Start Time</th>
                    <th>End Time</th>
                    <th>Room Number</th>
                    <th>Subject</th>
                    <th>Faculty Name</th>
                    <th>Make request</th>
                    <% if(slots.length!=0){
                        var i=1;
                        slots.forEach(function(data){
                    %>
                    <tr>
                        <td><%= i; %></td>
                        <td><%= data.Date %></td>
                        <td><%= data.Slot %></td>
                        <td><%= data.Starttime %></td>
                        <td><%= data.Endtime %></td>
                        <td><%= data.Roomnumber %></td>
                        <td><%= data.Subject %></td>
                        <td><%= data.Facultyname %></td>
                        <td><input type="radio" id="to_make_id" name="to_make_id" value="<%= data.ID %>"></td>
                    </tr>
                    <%  i++; }) }%>
                </table>
                <input type="hidden" name="req_id" id="req_id" value="<%= req_id %>">
                <input type="hidden" name="filename" id="filename" value="<%= filename %>">
                <div class="m-2 text-center"></div>
                    <button type="submit" class="m-2 btn btn-login float-left">Submit</button>
                </div>
            </form>
        </div>
    </main>


    <footer class="mt-auto">
        <div class="d-flex flex-row justify-content-around p-3">
            <div>
                <form action="/faq" method="GET">
                    <button class="btn" id="ftr_btn">FAQ's</button>
                </form>
            </div>
            <div>
                <form action="mailto:cb.en.u4cse18163@cb.students.amrita.edu" method="GET">
                    <button class="btn" id="ftr_btn">Mail Us</button>
                </form>
            </div>
            <div>
                <form action="/about" method="GET">
                    <button class="btn" id="ftr_btn">About</button>
                </form>
            </div>
        </div>
    </footer>


</body>

</html>